{
    "name": "daikon/value-object",
    "type": "library",
    "description": "Value-object components for Daikon-CQRS projects.",
    "keywords": ["value-object"],
    "homepage": "https://github.com/daikon-cqrs/value-object",
    "license": "MPL-2.0",
    "prefer-stable": true,
    "minimum-stability": "dev",
    "require": {
        "php": "^7.4",
        "daikon/interop": "master@dev",
        "daikon/data-structure": "master@dev",
        "ramsey/uuid": "^4.0"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.0",
        "sebastian/phpcpd": "^5.0",
        "squizlabs/php_codesniffer": "^3.3",
        "vimeo/psalm": "^3.0"
    },
    "autoload": {
        "psr-4": { "Daikon\\ValueObject\\" : "src" }
    },
    "autoload-dev": {
        "psr-4": { "Daikon\\Tests\\ValueObject\\": "tests" }
    },
    "archive": {
        "exclude": [
            "/build",
            "/vendor"
        ]
    },
    "scripts": {
        "build": [ "@sniff", "@tests" ],
        "code-sniffer": "phpcs -p -s --extensions=php --standard='PSR1,PSR2' src tests",
        "code-sniffer-autofix": "phpcbf --standard=PSR2 -d tabWidth=4 src tests",
        "copy-paste-detector": "phpcpd src tests || true",
        "psalm": "psalm --shepherd --show-info=true",
        "sniff": [
            "@code-sniffer",
            "@copy-paste-detector",
            "@validate-json",
            "@psalm"
        ],
        "test": "phpunit --no-coverage",
        "tests": "phpunit",
        "validate-json": "composer validate --no-check-all --no-check-lock",
        "validate-deps": "composer validate --with-dependencies --strict"
    }
}
